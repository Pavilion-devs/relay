# Configure the profile through AWS's browser-based login. Do not store access keys here.
AWS_PROFILE=relay
AWS_REGION=us-east-1
# Set to an accessible Bedrock model ID after account access is verified.
RELAY_MODEL_ID=
RELAY_DB=.data/relay.sqlite3
AWS_DEFAULT_REGION=us-east-1
